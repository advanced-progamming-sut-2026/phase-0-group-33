=== TEST BT-0605: the wizard turns a plant into a sheep
#
# Interaction : a zombie transforming a plant instead of destroying it
# Source      : project document, Dark Ages: sheep plants neither attack nor get eaten
# Note        : a failure means the sheep transformation is missing
#
# Legend:  '>' = a line typed into the game
#          '?' = a substring that must appear in the output (checked in order)
#          '#' = comment for the reader (ignored by the runner)
# --------------------------------------------------------------------------

> menu scoring-game
? Scoring game started
> add plant -t Sunflower
> start game
? The battle begins
> cheat add -n 9000 suns
> cheat remove-cooldown
> plant plant -t Sunflower -l (2,3)
> cheat spawn-zombie -t Wizard -l 7,3
> advance time -t 500 ticks
> show tile status -l (2,3)
? sheep
