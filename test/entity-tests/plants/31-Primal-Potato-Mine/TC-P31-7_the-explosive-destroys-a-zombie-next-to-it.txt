=== TEST P31-7: the explosive destroys a zombie next to it
#
# Entity    : Primal Potato Mine
# Damage column: 2400
# Base ability (database): مسلح شدن سریع‌تر ۵ ثانیه ای و انفجار مساحتی ۳x۳.
# Checks    : a normal zombie on the target tile does not survive
#
# Legend:  '>' = a line typed into the game
#          '?' = a substring that must appear in the output (checked in order)
#          '#' = comment for the reader (ignored by the runner)
# --------------------------------------------------------------------------

> menu scoring-game
? Scoring game started
> add plant -t Primal Potato Mine
> start game
? The battle begins
> cheat add -n 9000 suns
> cheat remove-cooldown
> cheat spawn-zombie -t Normal -l 3,3
> plant plant -t Primal Potato Mine -l (3,3)
> advance time -t 200 ticks
? Zombie of type Normal is dead
