=== TEST P26-7: the plant damages a zombie standing in its row
#
# Entity    : Kernel-pult
# Category  : Lobber
# Damage column: 20/40
# Damage per action: 20
# Action interval: 2.9s = 29 ticks
# Target    : Gargantuar, 3600 hit points, no armor
# Arithmetic: 150 ticks / 29 = 5 actions x 20 damage = 100 dealt
# Checks    : the target reports health 3500
#
# Legend:  '>' = a line typed into the game
#          '?' = a substring that must appear in the output (checked in order)
#          '#' = comment for the reader (ignored by the runner)
# --------------------------------------------------------------------------
#
# FIX (test-side only, game code untouched):
#   Kernel-pult fires either a kernel (20) or a butter (40), so the remaining
#   health of a target is not deterministic. The kill itself is checked instead.
# --------------------------------------------------------------------------

> menu scoring-game
? Scoring game started
> add plant -t Kernel-pult
> start game
? The battle begins
> cheat add -n 9000 suns
> cheat remove-cooldown
> plant plant -t Kernel-pult -l (1,3)
> cheat spawn-zombie -t Normal -l 9,3
> advance time -t 300 ticks
? Zombie of type Normal is dead
