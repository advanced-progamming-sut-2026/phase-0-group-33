=== TEST TC-1108-2: a match clears the plants and gives sun
#
# Rubric (Phase 1) : مینی‌گیم ها > Beghouled > ساخت ترکیب
# Group            : 11 - Minigames
# Kind             : automatic (RANDOM: the board layout)
# Goal             : 50 sun per match, more for longer lines.
#
# Legend:  '>' = a line typed into the game
#          '?' = a substring that must appear in the output (checked in order)
#          '#' = comment / explanation for the TA (ignored by the runner)
# ---------------------------------------------------------------------------

# NOTES
#   The scan tries every horizontal swap of the board; the ones that
#   create a line answer with 'Match! Combos so far: n/8 | Sun: ...'.
#   The sun balance at the end proves the matches paid out.
#
> register -u tester -p Abcd123! Abcd123! -n Tester -e tester@mail.com -g male
> pick question -q 1 -a blue -c blue
? Account created successfully. Please log in.
> login -u tester -p Abcd123!
? Login successful.
> menu cheat unlock-all
? CHEAT: everything unlocked.
> menu travel-log
? Redirected to travel-log menu
> play minigame -n beghouled -d 1
? (stage 1) started!
> show map
? Wave: 0/3

> swap -l (1, 1) (2, 1)
> swap -l (2, 1) (3, 1)
> swap -l (3, 1) (4, 1)
> swap -l (4, 1) (5, 1)
> swap -l (5, 1) (6, 1)
> swap -l (6, 1) (7, 1)
> swap -l (7, 1) (8, 1)
> swap -l (8, 1) (9, 1)
> swap -l (1, 2) (2, 2)
> swap -l (2, 2) (3, 2)
> swap -l (3, 2) (4, 2)
> swap -l (4, 2) (5, 2)
> swap -l (5, 2) (6, 2)
> swap -l (6, 2) (7, 2)
> swap -l (7, 2) (8, 2)
> swap -l (8, 2) (9, 2)
> swap -l (1, 3) (2, 3)
> swap -l (2, 3) (3, 3)
> swap -l (3, 3) (4, 3)
> swap -l (4, 3) (5, 3)
> swap -l (5, 3) (6, 3)
> swap -l (6, 3) (7, 3)
> swap -l (7, 3) (8, 3)
> swap -l (8, 3) (9, 3)
> swap -l (1, 4) (2, 4)
> swap -l (2, 4) (3, 4)
> swap -l (3, 4) (4, 4)
> swap -l (4, 4) (5, 4)
> swap -l (5, 4) (6, 4)
> swap -l (6, 4) (7, 4)
> swap -l (7, 4) (8, 4)
> swap -l (8, 4) (9, 4)
> swap -l (1, 5) (2, 5)
> swap -l (2, 5) (3, 5)
> swap -l (3, 5) (4, 5)
> swap -l (4, 5) (5, 5)
> swap -l (5, 5) (6, 5)
> swap -l (6, 5) (7, 5)
> swap -l (7, 5) (8, 5)
> swap -l (8, 5) (9, 5)
> show sun amount
? Sun:
> show map
? Wave: 0/3

# ---------------------------------------------------------------------------
# SAMPLE RUN - real output recorded from this project. This part of the game
# is random, so your numbers/positions will differ; the SHAPE must match.
# ---------------------------------------------------------------------------
#   Match! Combos so far: 1/8 | Sun: 100
#   Match! Combos so far: 2/8 | Sun: 150
