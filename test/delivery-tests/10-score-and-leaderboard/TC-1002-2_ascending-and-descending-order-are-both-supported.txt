=== TEST TC-1002-2: ascending and descending order are both supported
#
# Rubric (Phase 1) : امتیاز > لیدربورد > مرتب سازی براساس مورد خواسته شده
# Group            : 10 - Scoring game & leaderboard
# Kind             : automatic (deterministic)
# Goal             : -o asc / -o desc, with desc as the default.
#
# Legend:  '>' = a line typed into the game
#          '?' = a substring that must appear in the output (checked in order)
#          '#' = comment / explanation for the TA (ignored by the runner)
# ---------------------------------------------------------------------------

> register -u alpha -p Abcd123! Abcd123! -n Alpha -e a@mail.com -g male
> pick question -q 1 -a blue -c blue
? Account created successfully.
> menu exit
? Redirected to Signup menu
> register -u beta -p Abcd123! Abcd123! -n Beta -e b@mail.com -g female
> pick question -q 1 -a blue -c blue
? Account created successfully.
> login -u beta -p Abcd123!
? Login successful.
> menu cheat unlock-chapters
? All chapters and levels unlocked.
> menu leaderboard
? Redirected to leaderboard menu
> show leaderboard -s levels -o desc
? Username
> show leaderboard -s levels -o asc
? Username
