=== TEST TC-0213-2: each news source is distinguishable
#
# Rubric (Phase 1) : شروع بازی و منوها > منوی اخبار > نمایش عکس اخبار
# Group            : 02 - Game start & menus
# Kind             : manual (inspection by the TA)
# Goal             : Plant / zombie / level / minigame news can be told apart.
#
# Legend:  '>' = a line typed into the game
#          '?' = a substring that must appear in the output (checked in order)
#          '#' = comment / explanation for the TA (ignored by the runner)
# ---------------------------------------------------------------------------

# STEPS
#   1. Unlock a plant, meet a zombie, finish a level.
#   2. Run: menu news show-all
#
# EXPECTED RESULT
#   - 'New plant unlocked: ...' , 'New zombie encountered: ...' ,
#   - 'New level unlocked: ...' appear as separate, typed lines.
#   - Each of them maps to one picture in phase 2.
#
# (No '>' lines: this case is verified by looking at the repository /
#  by watching the program, not by the automatic runner.)
