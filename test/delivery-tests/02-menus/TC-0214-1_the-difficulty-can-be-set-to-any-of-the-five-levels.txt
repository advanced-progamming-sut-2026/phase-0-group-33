=== TEST TC-0214-1: the difficulty can be set to any of the five levels
#
# Rubric (Phase 1) : شروع بازی و منوها > منوی تنظیمات > امکان تغییر سختی بازی
# Group            : 02 - Game start & menus
# Kind             : automatic (deterministic)
# Goal             : Levels 1..5 are all accepted and named.
#
# Legend:  '>' = a line typed into the game
#          '?' = a substring that must appear in the output (checked in order)
#          '#' = comment / explanation for the TA (ignored by the runner)
# ---------------------------------------------------------------------------

> register -u tester -p Abcd123! Abcd123! -n Tester -e tester@mail.com -g male
> pick question -q 1 -a blue -c blue
? Account created successfully. Please log in.
> login -u tester -p Abcd123!
? Login successful.
> menu enter settings
? Redirected to settings menu
> menu settings change-difficulty -l 1
? Difficulty set to
> menu settings change-difficulty -l 2
? Difficulty set to
> menu settings change-difficulty -l 3
? Difficulty set to
> menu settings change-difficulty -l 4
? Difficulty set to
> menu settings change-difficulty -l 5
? Difficulty set to EXTREME.
