=== TEST TC-0203-1: passwords are stored hashed not in plain text
#
# Rubric (Phase 1) : شروع بازی و منوها > منوی ثبت‌نام > SHA-256 (امتیازی)
# Group            : 02 - Game start & menus
# Kind             : manual (inspection by the TA)
# Goal             : The users file never contains the typed password.
#
# Legend:  '>' = a line typed into the game
#          '?' = a substring that must appear in the output (checked in order)
#          '#' = comment / explanation for the TA (ignored by the runner)
# ---------------------------------------------------------------------------

# STEPS
#   1. Start the game and register a user with password Abcd123!.
#   2. Close the game.
#   3. Open the generated data/ folder next to where you started the game.
#   4. Search the user file for the text Abcd123!
#
# EXPECTED RESULT
#   - The literal password Abcd123! is NOT found anywhere in the file.
#   - Instead a 64-character hexadecimal SHA-256 digest is stored.
#   - See src/utils/PasswordHasher.java for the implementation.
#
# (No '>' lines: this case is verified by looking at the repository /
#  by watching the program, not by the automatic runner.)
