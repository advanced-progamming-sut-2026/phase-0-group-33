=== TEST TC-0103-2: UML diagram matches the delivered code
#
# Rubric (Phase 1) : زیرساخت > معماری مشخص
# Group            : 01 - Infrastructure
# Kind             : manual (inspection by the TA)
# Goal             : The UML produced in phase 0 is present and still describes the code.
#
# Legend:  '>' = a line typed into the game
#          '?' = a substring that must appear in the output (checked in order)
#          '#' = comment / explanation for the TA (ignored by the runner)
# ---------------------------------------------------------------------------

# STEPS
#   1. Open UML/UML.png (and UML/v2.puml).
#   2. Compare a few classes with src/models/entities/plant and .../zombie.
#
# EXPECTED RESULT
#   - The diagram shows the main classes and their relations.
#   - Class names in the diagram exist in the code.
#   - The zombie armor decorators appear as a Decorator relation.
#
# (No '>' lines: this case is verified by looking at the repository /
#  by watching the program, not by the automatic runner.)
