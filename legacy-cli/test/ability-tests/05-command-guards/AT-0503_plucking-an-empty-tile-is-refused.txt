=== TEST AT-0503: plucking an empty tile is refused
#
# Guard        : pluck plant must report that the tile holds nothing
# Measured     : There is no plant at (5, 5).
#
# Legend:  '>' = a line typed into the game
#          '?' = a substring that must appear in the output (checked in order)
#          '#' = comment for the reader (ignored by the runner)
# --------------------------------------------------------------------------

> menu scoring-game
? Scoring game started
> add plant -t Peashooter
> start game
? The battle begins
> cheat add -n 9000 suns
> cheat remove-cooldown
> pluck plant -l (5,5)
? There is no plant at (5, 5).
