=== TEST TC-1002-1: the board can be sorted by each column
#
# Rubric (Phase 1) : امتیاز > لیدربورد > مرتب سازی براساس مورد خواسته شده
# Group            : 10 - Scoring game & leaderboard
# Kind             : automatic (deterministic)
# Goal             : levels / minigames / quests / dailyquests / miopoint.
#
# Legend:  '>' = a line typed into the game
#          '?' = a substring that must appear in the output (checked in order)
#          '#' = comment / explanation for the TA (ignored by the runner)
# ---------------------------------------------------------------------------

> register -u tester -p Abcd123! Abcd123! -n Tester -e tester@mail.com -g male
> pick question -q 1 -a blue -c blue
? Account created successfully. Please log in.
> login -u tester -p Abcd123!
? Login successful.
> menu leaderboard
? Redirected to leaderboard menu
> show leaderboard -s levels
? Username
> show leaderboard -s minigames
? Username
> show leaderboard -s quests
? Username
> show leaderboard -s dailyquests
? Username
> show leaderboard -s miopoint
? Username
