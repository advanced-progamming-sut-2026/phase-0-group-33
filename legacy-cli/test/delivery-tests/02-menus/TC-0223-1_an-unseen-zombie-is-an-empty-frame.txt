=== TEST TC-0223-1: an unseen zombie is an empty frame
#
# Rubric (Phase 1) : شروع بازی و منوها > منوی کلکسیون > نمایش زامبی‌ها
# Group            : 02 - Game start & menus
# Kind             : automatic (deterministic)
# Goal             : Before meeting a zombie the collection hides its data.
#
# Legend:  '>' = a line typed into the game
#          '?' = a substring that must appear in the output (checked in order)
#          '#' = comment / explanation for the TA (ignored by the runner)
# ---------------------------------------------------------------------------

> register -u tester -p Abcd123! Abcd123! -n Tester -e tester@mail.com -g male
> pick question -q 1 -a blue -c blue
? Account created successfully. Please log in.
> login -u tester -p Abcd123!
? Login successful.
> menu enter collection
? Redirected to collection menu
> menu collection show-zombies
? You have not seen any zombies yet.
> menu collection show-zombie -z Gargantuar
? You have not encountered this zombie yet; its frame is empty.
