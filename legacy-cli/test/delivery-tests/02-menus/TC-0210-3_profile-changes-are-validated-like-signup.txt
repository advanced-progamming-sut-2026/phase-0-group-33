=== TEST TC-0210-3: profile changes are validated like signup
#
# Rubric (Phase 1) : شروع بازی و منوها > منوی پروفایل > تغییر فیلدها
# Group            : 02 - Game start & menus
# Kind             : automatic (deterministic)
# Goal             : A taken username, a weak password and a wrong old password are refused.
#
# Legend:  '>' = a line typed into the game
#          '?' = a substring that must appear in the output (checked in order)
#          '#' = comment / explanation for the TA (ignored by the runner)
# ---------------------------------------------------------------------------

> register -u other -p Abcd123! Abcd123! -n Other -e other@mail.com -g female
> pick question -q 1 -a blue -c blue
? Account created successfully.
> menu exit
? Redirected to Signup menu
> register -u tester -p Abcd123! Abcd123! -n Tester -e tester@mail.com -g male
> pick question -q 1 -a blue -c blue
? Account created successfully.
> login -u tester -p Abcd123!
? Login successful.
> menu enter profile
? Redirected to profile menu
> menu profile change-username -u other
? Username already taken.
> menu profile change-password -p abc -o Abcd123!
? Weak password
> menu profile change-password -p Zzzz123! -o nope
? Old password is incorrect.
