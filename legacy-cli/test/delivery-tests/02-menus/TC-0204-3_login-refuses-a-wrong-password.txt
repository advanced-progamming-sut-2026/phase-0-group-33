=== TEST TC-0204-3: login refuses a wrong password
#
# Rubric (Phase 1) : شروع بازی و منوها > منوی ورود > ورود
# Group            : 02 - Game start & menus
# Kind             : automatic (deterministic)
# Goal             : A wrong password prints its own error and keeps you in the login menu.
#
# Legend:  '>' = a line typed into the game
#          '?' = a substring that must appear in the output (checked in order)
#          '#' = comment / explanation for the TA (ignored by the runner)
# ---------------------------------------------------------------------------

> register -u tester -p Abcd123! Abcd123! -n Tester -e tester@mail.com -g male
> pick question -q 1 -a blue -c blue
? Account created successfully. Please log in.
> login -u tester -p Wrong123!
? Incorrect password.
> menu show current
? Login menu
