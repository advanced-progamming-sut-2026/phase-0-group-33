=== TEST TC-0302-1: every quest shows its progress counter
#
# Rubric (Phase 1) : کوئست‌ها > منوی ترول لاگ > نمایش میزان پیشرفت
# Group            : 03 - Quests
# Kind             : automatic (deterministic)
# Goal             : The [done/target] counter is printed next to each quest.
#
# Legend:  '>' = a line typed into the game
#          '?' = a substring that must appear in the output (checked in order)
#          '#' = comment / explanation for the TA (ignored by the runner)
#
# NOTE: the daily quests derive their numbers and their reward sizes from
#       the calendar date, so only the stable part of each line is asserted.
# ---------------------------------------------------------------------------

> register -u tester -p Abcd123! Abcd123! -n Tester -e tester@mail.com -g male
> pick question -q 1 -a blue -c blue
? Account created successfully. Please log in.
> login -u tester -p Abcd123!
? Login successful.
> menu travel-log
? Redirected to travel-log menu
> travel log page high
? - Only Cactus: kill
? - Hunter of Egypt: defeat 50 of its zombies
> travel log page daily
? - Daily sun collector: gather
