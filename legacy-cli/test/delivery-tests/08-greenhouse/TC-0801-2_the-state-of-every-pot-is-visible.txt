=== TEST TC-0801-2: the state of every pot is visible
#
# Rubric (Phase 1) : گلخانه > وضعیت کلی گلخانه > نمایش گلخانه
# Group            : 08 - Greenhouse
# Kind             : automatic (RANDOM: 50% Marigold / 50% one of your plants)
# Goal             : empty / locked / growing with the time left / ready.
#
# Legend:  '>' = a line typed into the game
#          '?' = a substring that must appear in the output (checked in order)
#          '#' = comment / explanation for the TA (ignored by the runner)
# ---------------------------------------------------------------------------

# NOTES
#   The plant is drawn at random. A Marigold needs 2h, one of your own
#   unlocked plants needs 8h. Re-run the case if you want to see the
#   other branch.
#
> register -u tester -p Abcd123! Abcd123! -n Tester -e tester@mail.com -g male
> pick question -q 1 -a blue -c blue
? Account created successfully. Please log in.
> login -u tester -p Abcd123!
? Login successful.
> menu greenhouse
? Redirected to greenhouse menu
> show greenhouse
? (1, 1): empty
> plant pot at (1, 1)
? hours to grow.
> show greenhouse
? ready in

# ---------------------------------------------------------------------------
# SAMPLE RUN - real output recorded from this project. This part of the game
# is random, so your numbers/positions will differ; the SHAPE must match.
# ---------------------------------------------------------------------------
#   Marigold planted at (1, 1); it needs 2 hours to grow.
#   (1, 1): Marigold [growing, ready in 2h 0m]
#   ---- the other 50% branch ----
#   Peashooter planted at (1, 1); it needs 8 hours to grow.
#   (1, 1): Peashooter [growing, ready in 8h 0m]
