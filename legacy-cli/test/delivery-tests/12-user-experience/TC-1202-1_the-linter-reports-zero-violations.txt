=== TEST TC-1202-1: the linter reports zero violations
#
# Rubric (Phase 1) : لینتر > بررسی خطاهای لینتر > تعداد خطا
# Group            : 12 - User experience
# Kind             : manual (inspection by the TA)
# Goal             : Checkstyle and PMD both run clean, as the doc requires.
#
# Legend:  '>' = a line typed into the game
#          '?' = a substring that must appear in the output (checked in order)
#          '#' = comment / explanation for the TA (ignored by the runner)
# ---------------------------------------------------------------------------

# STEPS
#   1. Run: gradlew.bat lint   (Linux/macOS: ./gradlew lint)
#   2. Open build/reports/checkstyle/main.html
#   3. Open build/reports/pmd/main.html
#
# EXPECTED RESULT
#   - BUILD SUCCESSFUL.
#   - 0 Checkstyle violations (line length, naming, method length).
#   - 0 PMD violations (unused code, NcssCount).
#
# (No '>' lines: this case is verified by looking at the repository /
#  by watching the program, not by the automatic runner.)
