=== TEST TC-0103-3: design patterns are used where the doc expects them
#
# Rubric (Phase 1) : زیرساخت > معماری مشخص
# Group            : 01 - Infrastructure
# Kind             : manual (inspection by the TA)
# Goal             : Decorator for zombie armor, Factory for zombie creation, Singleton for UserManager.
#
# Legend:  '>' = a line typed into the game
#          '?' = a substring that must appear in the output (checked in order)
#          '#' = comment / explanation for the TA (ignored by the runner)
# ---------------------------------------------------------------------------

# STEPS
#   1. Open src/models/entities/zombie/decorator/ZombieDecorator.java and its subclasses.
#   2. Open src/models/game/ZombieFactory.java.
#   3. Open src/controllers/managers/UserManager.java.
#
# EXPECTED RESULT
#   - Armor pieces (cone/bucket/brick/crown/newspaper/sarcophagus) are decorators.
#   - ZombieFactory builds a zombie together with its armor stack.
#   - UserManager exposes getInstance() and holds a single instance.
#
# (No '>' lines: this case is verified by looking at the repository /
#  by watching the program, not by the automatic runner.)
