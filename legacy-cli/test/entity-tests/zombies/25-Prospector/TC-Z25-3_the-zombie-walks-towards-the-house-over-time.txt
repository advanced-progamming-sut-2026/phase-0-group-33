=== TEST Z25-3: the zombie walks towards the house over time
#
# Entity    : Prospector
# Speed     : 0.16 tiles per second
# Checks    : after 15 seconds it has left column 9
# Documented ability: Carries dynamite that explodes after ten seconds and then walks backwards eating plants; an ice shot puts the fuse out.
#
# Legend:  '>' = a line typed into the game
#          '?' = a substring that must appear in the output (checked in order)
#          '#' = comment for the reader (ignored by the runner)
# --------------------------------------------------------------------------

> menu scoring-game
? Scoring game started
> add plant -t Sunflower
> start game
? The battle begins
> cheat add -n 9000 suns
> cheat remove-cooldown
> cheat spawn-zombie -t Prospector -l 9,3
> zombies info
?     position: 9, 3
> advance time -t 150 ticks
> zombies info
? Prospector:
