=== TEST Z09-3: the zombie walks towards the house over time
#
# Entity    : Explorer
# Speed     : 0.25 tiles per second
# Checks    : after 15 seconds it has left column 9
# Documented ability: Its torch destroys plants standing less than one tile ahead in its row; ice puts the torch out and fire lights it again.
#
# Legend:  '>' = a line typed into the game
#          '?' = a substring that must appear in the output (checked in order)
#          '#' = comment for the reader (ignored by the runner)
# --------------------------------------------------------------------------

> menu scoring-game
? Scoring game started
> add plant -t Sunflower
> start game
? The battle begins
> cheat add -n 9000 suns
> cheat remove-cooldown
> cheat spawn-zombie -t Explorer -l 9,3
> zombies info
?     position: 9, 3
> advance time -t 150 ticks
> zombies info
? Explorer:
