=== TEST Z19-9: upgrades a nearby plain zombie into a knight
#
# Entity    : King
# Documented ability: The King stands still in the rightmost column and every few seconds gives a crown and a shoulder plate to a plain zombie next to it.
# Source    : phase-1 project document
# Note      : a failure here means the documented ability is not modelled yet
#
# Legend:  '>' = a line typed into the game
#          '?' = a substring that must appear in the output (checked in order)
#          '#' = comment for the reader (ignored by the runner)
# --------------------------------------------------------------------------
#
# FIX (test-side only, game code untouched):
#   The promotion needs a plain Normal within 2 columns and 10 seconds.
# --------------------------------------------------------------------------

> menu cheat unlock-all
> menu enter chapter -c Dark Ages -l 1
? Entered Dark Ages, level 1
> add plant -t Sunflower
> start game
? The battle begins
> cheat spawn-zombie -t King -l 8,2
> cheat spawn-zombie -t Normal -l 8,2
> advance time -t 120 ticks
? The King knighted a zombie in lane 2!
> zombies info
? Knight:
?         crown: 1600
