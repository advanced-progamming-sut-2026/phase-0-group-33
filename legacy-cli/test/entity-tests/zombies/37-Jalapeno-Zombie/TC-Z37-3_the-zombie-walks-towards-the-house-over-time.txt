=== TEST Z37-3: the zombie walks towards the house over time
#
# Entity    : Jalapeno Zombie
# Speed     : 0.185 tiles per second
# Checks    : after 15 seconds it has left column 9
# Documented ability: Zombotany zombie shaped like a Jalapeno.
#
# Legend:  '>' = a line typed into the game
#          '?' = a substring that must appear in the output (checked in order)
#          '#' = comment for the reader (ignored by the runner)
# --------------------------------------------------------------------------
#
# FIX (test-side only, game code untouched):
#   The Jalapeno Zombie burns its lane and dies with it after ~10 seconds.
# --------------------------------------------------------------------------

> menu scoring-game
? Scoring game started
> add plant -t Peashooter
> start game
? The battle begins
> cheat spawn-zombie -t Jalapeno Zombie -l 9,2
> advance time -t 30 ticks
> zombies info
? Jalapeno Zombie:
?     position: 8.4, 2
